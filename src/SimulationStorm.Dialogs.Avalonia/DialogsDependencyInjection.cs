namespace SimulationStorm.Dialogs.Avalonia;

public class DialogsDependencyInjection
{
    // public static IServiceCollection AddDialogManager(this IServiceCollection services) => services
    //     .AddSingleton(GetAvaloniaDialogManager());

    // private static IDialogManager GetAvaloniaDialogManager() => new WindowDialogManager(GetTopLevel());
}